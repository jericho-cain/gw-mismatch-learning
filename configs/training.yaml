seed: 1234
synthetic_data:
  num_samples: 96
  input_dim: 128
  metric: cosine
  noise: 0.01
pairs:
  num_pairs: 1024
model:
  input_dim: 128
  embedding_dim: 8
  hidden_dims: [64, 32]
training:
  epochs: 5
  batch_size: 64
  learning_rate: 0.001
evaluation:
  top_k: 5
outputs:
  plot_dir: outputs/smoke
  save_plots: true
