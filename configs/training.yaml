seed: 1234
mock_data:
  num_samples: 64
  input_dim: 16
  noise: 0.01
pairs:
  num_pairs: 512
model:
  input_dim: 16
  embedding_dim: 4
  hidden_dims: [64, 32]
training:
  epochs: 5
  batch_size: 64
  learning_rate: 0.001
evaluation:
  top_k: 5
