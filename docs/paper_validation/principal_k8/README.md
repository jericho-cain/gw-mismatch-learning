# Principal k=8 validation

This isolated validation tree adopts latent dimension 8 with the original frozen seed
1234. Phase I, II, and III are complete under `outputs/principal_k8/`. The manuscript and
all frozen k=4 artifacts remain unchanged pending scientific review.
